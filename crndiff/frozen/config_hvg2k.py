# -*- coding: utf-8 -*-
"""hvg2k · OURS 的全部可调参数集中在这里，每个都写清楚是什么、为什么是这个值。

改实验只动这个文件。所有判据的触发值都记在 `hvg2k/RUN_LOG.md`。
"""
from __future__ import annotations

from pathlib import Path

# 077 债一(2026-08-24):ROOT 原本是 `Path(__file__).resolve().parents[3]`。
# HVG2K 树后来从 `CRNDIFF/Experiments/X/...` 搬到了
# `CRNDIFF/Conditional generation experiments/HVG2K/...`,`parents[3]` 于是指向
# 「Conditional generation experiments」,**两条派生路径同时失效**:
#     DATA_DIR = ROOT/_shared/data/all          -> 不存在
#     G61      = ROOT.parent/_shared/panel61    -> 不存在(真实位置 Experiments/_shared/panel61)
# 后果是纯 tilt 采样管线在当前 checkout 下根本起不来(= 复现性缺口,077 立案)。
# 修法:把 ROOT 钉回它当年实际所指的目录。实测在该值下 DATA_DIR 与 G61 均存在。
# 验收见 077 §债一.2:逐位复现一条已归档的 tilt 臂,sha256 必须与归档一致。
ROOT = Path(r"D:\research\Git-backup\crndiff\CRNDIFF\Experiments\X")
if not (ROOT / "_shared" / "data").is_dir():        # 搬家再发生时当场停,而不是静默算错
    raise SystemExit(f"config_hvg2k: ROOT does not resolve to the data tree: {ROOT}")

# =========================================================================== #
# 数据
# =========================================================================== #
DATASET = "all"          # "all"（11 类 451,513 细胞）或 "endo"（Endothelial 子集）
                         # 主线 2026-08-10：取消"先 endo pilot"，T2 直接在 all/ 上跑
DATA_DIR = ROOT / "_shared" / "data" / DATASET
GENE_HASH = "6f7963523c7a4c5bf1df7264c4c11e0a42fb0a9c4d52ca91d5724ae5e1eadb12"
# ↑ T0e 过滤后（HVG 之前去掉 MT-/RPL/RPS/MTRNR2L/HB*）的基因集哈希。
#   载入时断言，对不上直接停（红线 §5.3）。

# =========================================================================== #
# NMAX —— B3 判据
# =========================================================================== #
NMAX = 512
# 判据 (a)「被截断的**计数总质量** <= 0.5%」：
#     NMAX=128 -> 3.073%（不通过）  256 -> 0.888%（不通过）  **512 -> 0.284%（通过）**
#     1024 -> 0.149%   2048 -> 0.093%
#   取满足 (a) 的**最小**值 = 512。被截的基因 36 个，clip 之后实际损失质量 0.148%。
#
# 判据 (b)「e^-T · NMAX <= 0.1」**未采纳，见 RUN_LOG 的 [DEVIATION]**：
#   它在 T=T_O=5.2021 下要求 NMAX <= 18，与 (a) 直接冲突；而我们**已发表**的 10D 正文行
#   （linspace + T_O）本身就跑在 e^-T·NMAX = 13.09（D1）/ 5.82（D2）上，
#   即该判据被我们自己最好的配置违反 130 倍。改用从已发表配置标定出来的上界（见下）。

CLIP_X0_TO_NMAX = True
# 把 train 的计数在**喂进前向之前**就 clip 到 NMAX。
# 61gene 的 `train_ours_generic` 是 `binomial(原始 x0, r)` 再对**和**做 clamp，
#   即二项项用的是未截断的 n0。61gene 上 max_n0=481、NMAX=128，比值 3.8，无所谓；
#   hvg2k 是 9037 / 512 = 17.6，差一个数量级。
# clip 之后：前向、目标、类别头三者的支撑集一致都是 [0, NMAX]，
#   且「起点是否已忘掉 n0」的判据 e^-T·max_n0 里的 max_n0 就等于 NMAX，
#   = e^-5.2021 × 512 = **2.81**，落在已发表上界 6.52（10D D1 正文行）之内。
# 代价：0.284% 的计数质量（判据 (a) 已经接受的那一份），36 个基因的尾巴。

# =========================================================================== #
# V —— B2 判据
# =========================================================================== #
V_GRID_LO, V_GRID_HI = -10, 4      # V 量化到 2^-10 … 2^4 的 log2 网格
# 判据「sum V / lib_mean 落在 [0.95, 1.10]」：三个候选下限都通过，取最接近 1 的
#     2^-10 -> 1.0386（选它）   2^-8 -> 1.0410   2^-6 -> 1.0573
#   2^-10 同时被地板抬起的基因最少（219 个，占总质量 0.02%）。
#   实际用到 15 个不同的 V 值 —— bank 显存按 (tau, V) 缓存，这一列才是硬约束。
# 现行的 `max(1, 非零中位数)` 已废弃：它在 hvg2k 上 sum V/lib = 6.22，
#   98.1% 的基因偏出 sqrt(2)（61gene 上只有 6.6%，因为那里非零率 0.668 vs 这里 0.059）。

# =========================================================================== #
# 视界 T 与时间网格
# =========================================================================== #
GRID = "linspace"        # 主线 2026-08-10：全部实验统一 linspace，wake-sigma 废弃
F_BASE = 0.01            # 与 61gene/OURS/code/model.py 一致
T_HORIZON = "T_O"        # "T_O" 用谱估的视界；也可以直接给数字
T_O = 5.2021             # = 0.5*ln(sigma_1/sigma_noise)，在**过滤后的新基因集**上算
                         #   sigma_1 = 5093.6712，sigma_noise = 0.15436
# 主线撤销了我之前基于「步数分配 / sd-mean」两个代理量推荐 T=8 的意见，
# 理由是 10D 的直接实测（linspace 下 T_O 在 K=8 上比 T8 好 37%）与代理量矛盾，以实测为准。

# =========================================================================== #
# 网络（`_shared/code/backbone.py::AttnX0Posterior`，与 61gene 逐字同一个类）
# =========================================================================== #
D_MODEL, NHEADS, NLAYERS = 128, 4, 3
# B4 判据「头部参数量 <= 主干参数量的 5 倍」：head 是**逐 token 共享**的
#   `Linear(d_model, nmax+1)`，参数量与 dim 无关。
#   dim=2000 / nmax=512 时 head 66,177 / 主干 611,968 = **0.108×**，远远通过。
#   总参数 636,673（61gene）-> 934,401，只涨 1.47 倍。
# 任务书 B4 说的「Linear(hidden -> dim×(nmax+1))，3300 万到 1.3 亿」是
#   `vendor_ours.py::X0PosteriorJoint`（10Dtoy 用的类）的性质，实测 dim=2000 时 33,571,280 —
#   与面板/hvg2k 用的 attention 主干不是同一个类。**因此本轮不改架构。**

# =========================================================================== #
# 训练（配方照 61gene：EXP21::train_ours_generic + EXP134::STEPS）
# =========================================================================== #
BATCH = 128
# 2026-08-10 的教训：**batch=256 会跑死。**
# 200 步 smoke 上峰值 16.9 GB 看着安全，但那是 `max_memory_allocated`；
# 真正决定会不会抖动的是 **reserved**（nvidia-smi 看到的那个数），它随分配器碎片
# 一路爬到 23.6 / 24.5 GB，然后 WDDM 开始往共享内存溢出：
#   前 400 步 1.9 s/步 -> 之后约 20 s/步（GPU 报 100% 利用率但只有 103 W，是在等内存不是在算）。
# 5.3 小时只跑到 step 1000。降到 128（logits [128,2000,513] float32 = 0.52 GB），
# 并在启动脚本里设 PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True 对付碎片。
# 128×2000 = 25.6 万个 gene-token/步，仍是 61gene（1024×61 = 6.2 万）的 4 倍。
LR, WD, WARMUP = 2e-3, 1e-4, 500

# ---- EXP158-T3：训练提速。**只作用于训练循环。** ----
USE_BF16 = True
# `torch.autocast(bfloat16)`。**权重仍以 fp32 保存**，采样时 fp32 加载、fp32 前向，
# `post_fn` 输出仍是 float32（参考实现本来就是），核 bank / bridge 权重仍全程 float64。
# 所以对采样器的数值行为零影响 —— 主线 EXP158-T3 明确批准。
# `vendor_ours*.py` / `ours_fast_v2.py` / `bank_dedup.py` 一个字节没动。
USE_GRAD_CKPT = True
# 3 层 transformer 逐层 `torch.utils.checkpoint(use_reentrant=False)`。
# T2 实测注意力分数**没有**被物化（斜率 68.3 MB/batch，物化该是 183），
# 显存大头是 FFN 激活（3 层 × B×2000×512×4B）与 [B,2000,513] 的 logits，
# 检查点正好省的是前者。
EMA_DECAY = 0.999
STEPS_SMOKE = 200
STEPS_PILOT = 4000
# smoke 实测 **1.80 s/步**（batch=256，峰值显存 16.9 GB），所以 8000 步要 4 小时。
# 今晚要给 T3 采样留 ≥1 小时，改成 4000 步（约 2 小时）。判据仍是"连续 3 次 val
# 没改善就停"，跑不满是正常的。
#
# 为什么这么慢：**瓶颈在主干的自注意力，不在头**。注意力是基因数的平方：
#   61gene   61 个 token  -> 61² = 3.7k 个注意力分数
#   hvg2k  2000 个 token  -> 2000² = 4.0M 个，**1075 倍**
# 这是 61->2000 真正的扩展墙，B4 那个"头太大"的担心反而不成立（头是共享的）。
EVAL_EVERY = 200
VAL_CHUNK = 256
# val 评估的分块大小。**这是 2026-08-10 查出来的真正瓶颈**：
# 原来照抄 61gene 的写法，一次前向 2048 个细胞；在 dim=2000 上那是 **21.6 GB**
# （T2 实测推理 10.8 MB/细胞），比 batch=128 的训练步（8.5 GB）还大 2.5 倍。
# 分块到 256 -> 2.7 GB。用 reduction="sum" 累加再除总元素数，数值与一次性 mean 完全相同。
PATIENCE = 5
EARLY_STOP_REL = 0.003
EARLY_STOP_ENFORCE = False
# ★★ 2026-08-10 主线指令：**早停一律只记录、不执行**（`EARLY_STOP_ENFORCE=False`）。
#    第一次训一个 2000 维模型，信息比省时间重要；日志里照旧打
#    「若启用，第一次触发会在 step ____」，将来定预算用。
# ★ 教训一：第一版判据是 `val < best - 1e-4` 的**绝对**阈值 + patience=3，
#   在 val≈0.115 上 1e-4 只有 0.09% 的相对量，被噪声淹没。
#   改成**相对**阈值 + patience=5（≈1000 步的观察窗）。
# ★ 教训二（真正的 bug，把 run1/run1b 都砍断了）：改成相对阈值之后，
#   `best` 既当「存盘用的最优」又当「相对阈值的参照点」，而 `best=min(best,vl)`
#   每次刷新都往下棘轮 —— 于是**刷新了 best 但幅度不到 0.3% 的那一步**，
#   best 被更新、bad 却 +1，破纪录的一步没有清零计数器：
#       run1  step 5000  val=0.11510 < best=0.11520（+0.087%）-> bad=3/3
#       run1b step 2800  val=0.11408 == 最终 best_val 本身      -> bad=5/5
#   修在 `model_hvg2k.py`：参照点 `es_ref` 与存盘用的 `best` **拆成两个变量**，
#   `es_ref` 只在显著改善时下移。修完也不启用（见上）。
SEED_PILOT = 20260625

# =========================================================================== #
# 采样 / 评测
# =========================================================================== #
N_GEN = 20000
K_LADDER_PILOT = [8, 32, 128]
SAMP_SEEDS = [20260901, 20260902]
