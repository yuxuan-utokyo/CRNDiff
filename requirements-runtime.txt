# Core training and sampling. Install a PyTorch build appropriate for your hardware.
-r requirements.txt
torch>=2.0
scikit-learn>=1.3
joblib>=1.3

# scVI/scANVI, CellTypist evaluation and CFGen have additional dependencies;
# see README.md and baselines/README.md. CFGen uses its pinned upstream environment.
