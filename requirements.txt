# Offline reproduction of the tables and the toy/horizon figures needs only these:
numpy>=1.24
scipy>=1.10
matplotlib>=3.7
pandas>=2.0
pillow>=9.0

# Training, sampling and scoring additionally need:
# torch>=2.0
# scikit-learn>=1.3
# joblib>=1.3
# anndata>=0.10
# scanpy>=1.10
# celltypist>=1.6
# scvi-tools>=1.1        # scVI and scANVI baselines only
